%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%    Basic packages and main info. 

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[8pt,a2paper,dvipsnames]{article}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel} % es-lcroman es para solucionar el etiquetado de los enumerate en roman, que siempre salen en mayúscula si no.
%\decimalpoint
\usepackage{amsmath,amsfonts,amssymb,graphicx,bigints,setspace,multirow,
esvect,esdiff,eqparbox,tabularx,mathtools,empheq,caption,braket,color}
\captionsetup{justification=centering,format=plain}
\usepackage[a4paper,hmargin=3cm,vmargin={2.5cm,2.5cm}]{geometry}
%\setlength{\footskip}{1.5cm}
\addtolength{\footnotesep}{2mm}
\title{\vspace{-2cm}\textbf{Final Project report \duda{?}}}
\date{}
\usepackage[x11names]{xcolor} %x11names usa los colores de x11, para consultar los nombres ir a la docu de xcolor
\usepackage[]{hyperref}
\hypersetup{
    pdftitle={Final Project},
	pdfauthor={Javier Fernández // jfernandil@alumnos.unex.es},
	pdfsubject={Electronis Lab},
	%pdfkeywords={},
	colorlinks = false,
	linkbordercolor = DarkGoldenrod2,
	urlbordercolor= DarkGoldenrod2,
	%linkcolor = black,
	%urlcolor  = black,
}
\author{\duda{?} \\ \duda{?} \\ Javier Fernández Aparicio $\blacktriangleright$ \href{mailto:jfernandil@alumnos.unex.es}{jfernandil@alumnos.unex.es} \\Constantino Sánchez Romero $\blacktriangleright$ \href{mailto:csanchezgm@alumnos.unex.es}{csanchezgm@alumnos.unex.es}}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%     PAQUETES PARTICULARES COMENTADOS:

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%\usepackage{electrum}
%\usepackage[T1]{fontenc}

\usepackage{lscape}

\usepackage{overpic} 																		%Para poder poner texto a las figuras basándose en una cuadrícula para posicionarlo.

\usepackage[x11names]{xcolor} 																%x11names usa los colores de x11, para consultar los nombres ir a la docu de xcolor.

\usepackage{tikz}
\usetikzlibrary{shapes,arrows,chains}

\usepackage[makeroom]{cancel} 																%Para tachar partes de fórmulas.

\usepackage[arrowdel]{physics} 															%Para añadir la flecha de vector a las divergencias y los gradientes.

\usepackage{soulutf8} 																		%Paquete para subrayar correctamente (y obtener saltos de línea automáticos).

%\usepackage{lmodern} 																		%Scalable font // CUIDADO CON ESTE PAQUETE QUE PUEDE GENERAR QUE NO SE CARGUE LA FUENTE. NO SÉ SI EL DECLAREMATHSIZES DE DEBAJO FUNCIONARÁ AHORA.
%\DeclareMathSizes{10}{10}{7}{5} 															%default
\DeclareMathSizes{10}{10}{6}{5} 															%{tamaño-texto}{tamaño-math}{tamaño-subíndice/superíndice}{tamaño-subsubíndice/supersuperíndice} Para escalar los subíndices, me respondieron en un post de 
																							%StackExchange.

\usepackage{cleveref} 																		%Referencias inteligentes.

\usepackage{chngcntr} 																		%Modificar contadores de las etiquetas, creo.

\usepackage{bookmark} 																		%Paquete para añadir el índice a los marcadores interactivos del PDF.

\usepackage{enumitem} 																		%Paquete para hacer listas enumeradas por números romanos.

\usepackage{siunitx} 																		%Para escribir unidades del SI.

\usepackage{csquotes} 																		%Para poner comillas mejor.
\setquotestyle{english} 																	%Luego se establece ese estilo para todo el documento. Así no hace falta que use la variante \enquote*{}

\usepackage{adjustbox} 																		%Para ajusta los gráficos a la parte superior del entorno minipage (creo).

\usepackage{xfrac} 																			%Son dos formas de poner fracciones, en el caso de usar este el operador es \sfrac{}{}

\usepackage{transparent} 																	%Algo relacionado con la trasparencia en gráficos, puede que de los colores. Algo tipo canal alpha.

\usepackage{mathrsfs} 																		%Mathematical "script" Letters.

\usepackage{epstopdf} 																		%Para convertir los gráficos eps del gnuplot a pdf, creo.

\usepackage{bm} 																			%Texto en negrita más automatizado.

\usepackage{etoolbox} 																		%No tengo muy claro si este paquete tiene relación con el código q no estoy usando justo debajo.

%\newcommand{\sect}[1]{ 																	%Comando que se utilizó para añadir una sección a la tabla de contenidos manualmente e incrementar el contador de las secciones manualmente, esto lo hago 
%	\section*{#1}																			%porque cuando declaras la sección con el asterisco no solo no te las numera, si no que no incrementa el contador de las secciones y esto para las subsecciones 
																							%es contraproducente, lo estoy haciendo así porque no quiero que me numere las secciones explícitamente ya que las estoy usando como temas y al llamarlas "Tema 
%	\addcontentsline{toc}{section}{#1}														%x" queda feo el número delante. DE AHORA EN ADELANTE SE USARÁ ESTE NUEVO COMANDO PARA CREAR SECCIONES, ASÍ QUE NO SE VERÁN REMARCADAS EN AZUL.% 
%    \refstepcounter{section}
%}

\usepackage{pgfplots} %Generar gráficos con código en Latex directamente.
\pgfplotsset{compat=1.14}
\usepgfplotslibrary{units}
\makeatletter
\pgfplotsset{
  unit code/.code 2 args=
    \begingroup
    \protected@edef\x{\endgroup\si{#2}}\x
} 
\makeatother

\usepackage{hepparticles} 																	%Paquetes útiles para la notación de física de partículas.

\usepackage{tensor} 																		%Para poder poner subíndices y superíndices a la izda también

\usepackage[thinlines]{easytable} 															%para gestionar mejor el entorno tabular

\usepackage{slashbox}

\usepackage{colortbl}

%-------------------------
% Para un encabezado más bonito

%\renewcommand{\subsubsectionmark}[1]{\markright{\thesubsubsection.\ #1}} 				%Se usa para darle al marcador del encabezado la profundidad de subsección, si se descomenta, 
																							%pero no es muy útil ya que al hacerlo así es difícil es posible crear algo de confisuón en el encabezado, 
																							%a veces las subsecciones empiezan a mitad de página. Para usarlo en lugar de \leftmark tendría que descomentar ese renew y usar abajo \rightmark.
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[R]{\thepage}
\renewcommand{\headrulewidth}{0pt}
%-------------------------

\usepackage{pdfpages}

%\usepackage{lipsum} 																		%Usar el testo lore ipsum para ejemplo".

%\usepackage{schemata} 																		%Hacer esquemas con llaves de tipo {, De momento no lo estoy usando.
%\newcommand\AB[2]{\schema{\schemabox{#1}}{\schemabox{#2}}}

%\usepackage[cleanup]{gnuplottex} 															%Para incluir código de gnuplot y poder usar código de latex en las etiquetas.

%\usepackage{nicefrac} 																		%Para escribir las funciones de forma más estética, ahora estoy usando el otro paquete, xfrac.

%\usepackage{relsize} 																		%Para usar _{\!\mathsmaller{A}} en los subíndices, no es necesario pero puede ser útil.

%\usepackage{breqn} 																		%no sé para qué vale, parecía usarlo en Hepparticles, pero no parece necesario y ralentiza y congela la compilación.
																							%% Fix the wrong assignment to \nu
%\DeclareFlexSymbol{\nu}{Ord}{greek}{17}
%
%\catcode`_=12 																				%Para los subíndices a la izda, debe incluirse después del paquete breqn. Parece que breqn redefine _ y ^, por eso estas dos líneas anteriores.
%\catcode`^=12


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%				CONTADORES:

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%



\counterwithin*{equation}{section}
\counterwithin*{equation}{subsection}
\counterwithin*{equation}{subsubsection}
\counterwithin*{equation}{enumi}

\counterwithin*{figure}{section}
\counterwithin*{figure}{subsection}
\counterwithin*{figure}{enumi}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%	COMANDOS PROPIOS Y CONFIGURACIONES INICIALES:

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%



\setlength\arrayrulewidth{1pt}
\setul{.5pt}{.5pt}


\newcommand{\undbf}[1] {\textbf{\ul{#1}}} 									%Comando para los subrrayados en negrita \smash además permite que la línea atraviese las letras con parte baja, como p, g, j, etc ... 

\newcommand{\resaltar}[1] {\textbf{{\color{ForestGreen}#1}}} 				%Comando para dar color y negrita.

\newcommand{\duda}[1] {\textbf{\color{red}#1}} 							%Comando para resaltar dudas de forma provisional.

\newcommand{\vdiff}[1] {\dd\vec{\mathbf{#1}}} 								%Comando para dibujar vectores en un diferencial de una integral.

\newcommand{\prr}{\\[0.5cm]} 												%Comando para separación estandar entre párrafos o ecuaciones.

\newcommand{\prrm}{\\[0.25cm]} 											%La mitad de \prr

\newcommand{\sepeq}{\hspace{0.5cm};\hspace{0.5cm}} 						%Para separar ecuaciones por un punto y coma.

\newcommand{\vinc}[1] {\textbf{\color{NavyBlue}#1}}

\newcommand{\e}[1]{e^{\left( #1 \right)}}

\newcommand{\K}{\si\kelvin}

\newcommand{\eV}{\si\electronvolt} 

\newcommand{\MeV}{\si{\mega\electronvolt}} 

\newcommand{\fermi}{\si{\femto\metre}}

\newcommand{\mevporfermi}{\si{\mega\electronvolt\femto\metre}}

\newcommand{\cm}{\si{\centi\metre}} 

\newcommand{\ccm}{\si{\cubic\centi\metre}}

\newcommand{\pccm}{\si{\per\cubic\centi\metre}}

\newcommand{\A}{\si\angstrom}

\newcommand{\scmpVps}{\si{\square\centi\metre\per\volt\per\second}}


\newcommand{\mathsize}[1] {\text{\fontsize{15pt}{5mm}$ #1 $}} 								%Este comando que me he fabricado: (\mathsize) sirve para redimensionar las partes de algunas fórmulas, en concreto las indicaciones de algunos \underbrace

% Comando para colorear las cajas con las que se recuadran las ecuaciones
% Syntax: \colorboxed[<color model>]{<color specification>}{<math formula>}
\newcommand*{\colorboxed}{}
\def\colorboxed#1#{%
  \colorboxedAux{#1}%
}
\newcommand*{\colorboxedAux}[3]{%
  % #1: optional argument for color model
  % #2: color specification
  % #3: formula
  \begingroup
    \colorlet{cb@saved}{.}%
    \color#1{#2}%
    \boxed{%
      \color{cb@saved}%
      #3%
    }%
  \endgroup
}

\makeatletter																				%Raíces cuadradas cerradas
\let\oldr@@t\r@@t
\def\r@@t#1#2{%
\setbox0=\hbox{$\oldr@@t#1{#2\,}$}\dimen0=\ht0
\advance\dimen0-0.2\ht0
\setbox2=\hbox{\vrule height\ht0 depth -\dimen0}%
{\box0\lower0.4pt\box2}}
\makeatother
\usepackage{letltxmacro}
\LetLtxMacro{\oldsqrt}{\sqrt}
\renewcommand*{\sqrt}[2][\ ]{\oldsqrt[#1]{#2}}


\def\mean#1{\left< #1 \right>} %Para hacer más rápido los valores esperados

%----------------------------------
%Todo esto de debajo no me gusta, es un intento de usar siunitx para crear unidades de energía y momento en física nuclear y de partículas, pero es preferible usar el paquete "hepunits"

\DeclareSIUnit\clight{\text{\ensuremath{c}}} 												%Para declarar la variable \clight del paquete siuintx sin el subíndice 0 que lleva x defecto

\newcommand{\MeVsC}{\si[per-mode=symbol]{\mega\electronvolt\per\clight\squared}} 			%Para poner los mev/c2 (MASAS), más sencillo
%\def\MeVC2{\si[per-mode=symbol]{\mega\electronvolt\per\square\clight}} 					%En el \newcommand no se pueden poner números para el nombre del comando. CUIDADO, ESTE NO FUNCIONA DEL TODO BIEN.
%\def\MeVC2#1{\SI[per-mode=symbol]{ #1 }{\mega\electronvolt\per\clight\squared}} 			%Lo mismo pero funciona de otra forma, \MeVC2{2}

\newcommand{\MeVC}{\si[per-mode=symbol]{\mega\electronvolt\per\clight}} 					%Para poner los mev/c (MOMENTOS LINEALES) más sencillo
%\def\MeVC{\si[per-mode=symbol]{\mega\electronvolt\per\clight}} 							%Ya que hago el anterior con \def, este también, además al hacerlo así se debe usar \si en lugar de \SI
%\def\MeVC#1{\SI[per-mode=symbol]{ #1 }{\mega\electronvolt\per\clight}} 					%Otra forma, pero en este caso es para poner \MeVC{2}, por ejemplo

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%	ETIQUETADO DE SECCIONES Y ECUACIONES

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%



%\renewcommand{\thesubsubsection} {\thesubsection.\alph{subsubsection}} %to enumerate subsubsections with letters%

\newif\ifinenumerate
\AtBeginEnvironment{enumerate}{\inenumeratetrue}

%Todo lo siguiente se utiliza para numerar las ecuaciones de la forma <sección.subsección.enumerate.ecuación> y ademaś hacer corresponder las etiquetas con las anclas del hyperref UNÍVOCAMENTE sin que interfiera el formato de la etiqueta de enum, que puede ser \textit{} perfectamente 
%\makeatletter
%\renewcommand\theequation{%
%  \ifnum\value{subsubsection}>0 \thesubsubsection.\else
%  \ifnum\value{subsection}>0 \thesubsection.\else
%  \ifnum\value{section}>0 \thesection.\else
%  \ifinenumerate \csname theenum\romannumeral\the\@enumdepth\endcsname\fi\fi\fi\fi
%  \arabic{equation}%
%}
%\AtBeginDocument{%
%  \renewcommand\theHequation{%
%    \ifnum\value{subsubsection}>0 \theHsubsubsection.\else
%    \ifnum\value{subsection}>0 \theHsubsection.\else
%    \ifnum\value{section}>0 \theHsection.\else
%    \ifinenumerate \csname theHenum\romannumeral\the\@enumdepth\endcsname.\fi\fi\fi\fi
%    \arabic{equation}%
%  }%
%}
%
%%Esto es lo mismo que lo anterior pero para las figuras.
%\renewcommand\thefigure{%
%  \ifnum\value{subsubsection}>0 \thesubsubsection.\else
%  \ifnum\value{subsection}>0 \thesubsection.\else
%  \ifnum\value{section}>0 \thesection.\else
%  \ifinenumerate \csname theenum\romannumeral\the\@enumdepth\endcsname\fi\fi\fi\fi
%  \arabic{figure}%
%}
%\AtBeginDocument{%
%  \renewcommand\theHfigure{%
%    \ifnum\value{subsubsection}>0 \theHsubsubsection.\else
%    \ifnum\value{subsection}>0 \theHsubsection.\else
%    \ifnum\value{section}>0 \theHsection.\else
%    \ifinenumerate \csname theHenum\romannumeral\the\@enumdepth\endcsname.\fi\fi\fi\fi
%    \arabic{figure}%
%  }%
%}
%\makeatother



\begin{document}
\maketitle
\noindent
Starting.







\end{document}
